import React, { Component } from "react";

class Signature extends Component {
  componentDidMount() {
    let tag = `Vīta brevis, ars longa.`;

    console.log(tag);
  }

  render() {
    return [];
  }
}

export default Signature;
